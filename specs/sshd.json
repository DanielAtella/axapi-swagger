{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - SSHD",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the sshd category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/sshd": {
      "parameters": [],
      "get": {
        "tags": [
          "SSHD"
        ],
        "description": "SSHD service operation",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/sshd"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "SSHD"
        ],
        "description": "SSHD service operation",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/sshd"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/sshd"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "sshd": {
        "type": "object",
        "properties": {
          "restart": {
            "type": "number",
            "description": "Restart SSH service",
            "default": 0
          },
          "wipe": {
            "type": "number",
            "description": "Wipe SSH key",
            "default": 0
          },
          "regenerate": {
            "type": "number",
            "description": "Wipe and generate SSH key",
            "default": 0
          },
          "generate": {
            "type": "number",
            "description": "Generate SSH key",
            "default": 0
          },
          "re-add-rsa": {
            "type": "string",
            "description": "remove ip address from known_hosts",
            "minLength": 1,
            "maxLength": 128
          },
          "size": {
            "type": "string",
            "description": "'2048': Key size 2048bit; '4096': Key size 4096bit; ",
            "enum": [
              "2048",
              "4096"
            ]
          },
          "load": {
            "type": "number",
            "description": "Load SSH key",
            "default": 0
          },
          "use-mgmt-port": {
            "type": "number",
            "description": "Use management port as source port",
            "default": 0
          },
          "file-url": {
            "type": "string",
            "description": "File URL"
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}