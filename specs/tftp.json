{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - TFTP",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the tftp category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/tftp": {
      "parameters": [],
      "get": {
        "tags": [
          "TFTP"
        ],
        "description": "TFTP client configuration",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/tftp"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "TFTP"
        ],
        "description": "TFTP client configuration",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/tftp"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/tftp"
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "TFTP"
        ],
        "description": "TFTP client configuration",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/tftp"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/tftp"
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "TFTP"
        ],
        "description": "TFTP client configuration",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/tftp"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "tftp": {
        "type": "object",
        "properties": {
          "blksize": {
            "type": "number",
            "description": "TFTP client block size value (Block size (Blksize/Max file size. Example: 1K/64M, 8K/512M, 32K/2048M),default is 32768)",
            "default": 32768,
            "minimum": 512,
            "maximum": 32768
          },
          "uuid": {
            "type": "string",
            "description": "uuid of the object",
            "minLength": 1,
            "maxLength": 64
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}