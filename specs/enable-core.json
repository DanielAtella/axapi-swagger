{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - ENABLE-CORE",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the enable-core category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/enable-core": {
      "parameters": [],
      "get": {
        "tags": [
          "ENABLE-CORE"
        ],
        "description": "Enable system coredump switch",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/enable-core"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "ENABLE-CORE"
        ],
        "description": "Enable system coredump switch",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/enable-core"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/enable-core"
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "ENABLE-CORE"
        ],
        "description": "Enable system coredump switch",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/enable-core"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/enable-core"
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "ENABLE-CORE"
        ],
        "description": "Enable system coredump switch",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/enable-core"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "enable-core": {
        "type": "object",
        "properties": {
          "core-level": {
            "type": "string",
            "description": "'a10': Enable A10 core dump, by default; 'system': Enable system coredump; ",
            "default": "a10",
            "enum": [
              "a10",
              "system"
            ]
          },
          "uuid": {
            "type": "string",
            "description": "uuid of the object",
            "minLength": 1,
            "maxLength": 64
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}