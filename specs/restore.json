{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - RESTORE",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the restore category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/restore": {
      "parameters": [],
      "get": {
        "tags": [
          "RESTORE"
        ],
        "description": "Restore system files",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/restore"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "RESTORE"
        ],
        "description": "Restore system files",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/restore"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/restore"
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "RESTORE"
        ],
        "description": "Restore system files",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/restore"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "restore": {
        "type": "object",
        "properties": {
          "use-mgmt-port": {
            "type": "number",
            "description": "Use management port as source port",
            "default": 0
          },
          "remote-file": {
            "type": "string",
            "description": "Remote file path"
          },
          "password": {
            "type": "string",
            "description": "password for the remote site",
            "minLength": 1,
            "maxLength": 128
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}