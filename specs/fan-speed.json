{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - FAN-SPEED",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the fan-speed category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/fan-speed": {
      "parameters": [],
      "get": {
        "tags": [
          "FAN-SPEED"
        ],
        "description": "Control FAN Speed setting",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/fan-speed"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "FAN-SPEED"
        ],
        "description": "Control FAN Speed setting",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/fan-speed"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/fan-speed"
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "FAN-SPEED"
        ],
        "description": "Control FAN Speed setting",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/fan-speed"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/fan-speed"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "fan-speed": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "'auto': Set FAN Speed to automatic based on system temperature; '50': Set FAN Speed to 50% of maximum speed (approx); '75': Set FAN Speed to 75% of maximum speed (approx); '100': Set FAN Speed to 100% full hardware speed; ",
            "default": "auto",
            "enum": [
              "auto",
              "50",
              "75",
              "100"
            ]
          },
          "uuid": {
            "type": "string",
            "description": "uuid of the object",
            "minLength": 1,
            "maxLength": 64
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}