{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - RELOAD",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the reload category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/reload": {
      "parameters": [],
      "get": {
        "tags": [
          "RELOAD"
        ],
        "description": "Reload the system without rebooting",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/reload"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "RELOAD"
        ],
        "description": "Reload the system without rebooting",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/reload"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/reload"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "reload": {
        "type": "object",
        "properties": {
          "device": {
            "type": "number",
            "description": "Reload a specific device when VCS is enabled (device id)"
          },
          "all": {
            "type": "number",
            "description": "Reload all devices when VCS is enabled, or only this device itself if VCS is not enabled",
            "default": 0
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}