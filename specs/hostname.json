{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - HOSTNAME",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the hostname category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/hostname": {
      "parameters": [],
      "get": {
        "tags": [
          "HOSTNAME"
        ],
        "description": "Set system's network name",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/hostname"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "HOSTNAME"
        ],
        "description": "Set system's network name",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/hostname"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/hostname"
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "HOSTNAME"
        ],
        "description": "Set system's network name",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/hostname"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/hostname"
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "HOSTNAME"
        ],
        "description": "Set system's network name",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/hostname"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "hostname": {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "description": "This System's Network Name",
            "minLength": 1,
            "maxLength": 31
          },
          "uuid": {
            "type": "string",
            "description": "uuid of the object",
            "minLength": 1,
            "maxLength": 64
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}