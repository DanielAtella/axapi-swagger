{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - POAP",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the poap category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/poap": {
      "parameters": [],
      "get": {
        "tags": [
          "POAP"
        ],
        "description": "Set POAP; when next boot, if startup-config is empty, system will enter POAP Mode",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/poap"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "POAP"
        ],
        "description": "Set POAP; when next boot, if startup-config is empty, system will enter POAP Mode",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/poap"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/poap"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "poap": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "'enable': Enable POAP mode; 'disable': Disable POAP mode; ",
            "enum": [
              "enable",
              "disable"
            ]
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}