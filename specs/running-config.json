{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - RUNNING-CONFIG",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the running-config category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/running-config": {
      "parameters": [],
      "get": {
        "tags": [
          "RUNNING-CONFIG"
        ],
        "description": "Configure the behaviour of show running config",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/running-config"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "RUNNING-CONFIG"
        ],
        "description": "Configure the behaviour of show running config",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/running-config"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/running-config"
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "RUNNING-CONFIG"
        ],
        "description": "Configure the behaviour of show running config",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/running-config"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/running-config"
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "RUNNING-CONFIG"
        ],
        "description": "Configure the behaviour of show running config",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/running-config"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "running-config": {
        "type": "object",
        "properties": {
          "aflex": {
            "type": "number",
            "description": "Show aFleX scripts",
            "default": 0
          },
          "class-list": {
            "type": "number",
            "description": "Show class-list files",
            "default": 0
          },
          "uuid": {
            "type": "string",
            "description": "uuid of the object",
            "minLength": 1,
            "maxLength": 64
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}