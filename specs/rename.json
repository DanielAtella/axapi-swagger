{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - RENAME",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the rename category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/rename": {
      "parameters": [],
      "get": {
        "tags": [
          "RENAME"
        ],
        "description": "Rename Object Name",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/rename"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "RENAME"
        ],
        "description": "Rename Object Name",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/rename"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/rename"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "rename": {
        "type": "object",
        "properties": {
          "object": {
            "type": "string",
            "description": "Lineage of object being renamed e.g: slb.server, slb.service-group, slb.virtual-server",
            "minLength": 1,
            "maxLength": 128
          },
          "instance-name": {
            "type": "string",
            "description": "Old Instance Name",
            "minLength": 1,
            "maxLength": 127
          },
          "new-instance-name": {
            "type": "string",
            "description": "New Instance Name",
            "minLength": 1,
            "maxLength": 127
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}