{
  "openapi": "3.0.0",
  "info": {
    "title": "A10 aXAPI v3 Documentation - MAXIMUM-PATHS",
    "version": "3.0.0",
    "description": "Auto-generated documentation for the maximum-paths category."
  },
  "servers": [
    {
      "url": "https://{thunder_ip}/axapi/v3",
      "variables": {
        "thunder_ip": {
          "default": "thunder.example.com",
          "description": "IP address or hostname of the Thunder device"
        }
      }
    }
  ],
  "paths": {
    "/axapi/v3/maximum-paths": {
      "parameters": [],
      "get": {
        "tags": [
          "MAXIMUM-PATHS"
        ],
        "description": "Set maximum number of route multipaths installed into FIB",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/maximum-paths"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "MAXIMUM-PATHS"
        ],
        "description": "Set maximum number of route multipaths installed into FIB",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/maximum-paths"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/maximum-paths"
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "MAXIMUM-PATHS"
        ],
        "description": "Set maximum number of route multipaths installed into FIB",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/maximum-paths"
                }
              }
            }
          }
        },
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/maximum-paths"
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "MAXIMUM-PATHS"
        ],
        "description": "Set maximum number of route multipaths installed into FIB",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/maximum-paths"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "maximum-paths": {
        "type": "object",
        "properties": {
          "path": {
            "type": "number",
            "description": "supported multipath numbers",
            "default": 4,
            "minimum": 1,
            "maximum": 64
          },
          "uuid": {
            "type": "string",
            "description": "uuid of the object",
            "minLength": 1,
            "maxLength": 64
          }
        }
      }
    },
    "securitySchemes": {
      "SessionToken": {
        "type": "apiKey",
        "in": "header",
        "name": "Authorization",
        "description": "ACOS Session Token (e.g., A10 {token})"
      }
    }
  },
  "security": [
    {
      "SessionToken": []
    }
  ]
}